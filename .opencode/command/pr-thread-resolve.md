---
description: Reply to PR review threads and resolve them only after the reply is posted and verified. Enforces approval gates, never deletes.
---

Use the `pr-thread-resolve` skill. Invoke the skill and follow its instructions exactly.

Target: $ARGUMENTS
