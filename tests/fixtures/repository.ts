import type { Repository } from '@/src/types/repository';

export const testRepository: Repository = {
    owner: 'john-doe',
    repo: 'test-repo',
};

export const adapterRepository: Repository = { owner: 'octo', repo: 'repo' };
